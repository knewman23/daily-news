instaloader>=4.14
browser_cookie3>=0.19        # for `instaloader --load-cookies`; direct login is blocked
faster-whisper>=1.1
pyobjc-framework-Vision>=12.0     # OCR for image posts, via macOS Vision (local, free)
pyobjc-framework-Quartz>=12.0
markdown>=3.7
pyyaml>=6.0
pytest>=8.0
